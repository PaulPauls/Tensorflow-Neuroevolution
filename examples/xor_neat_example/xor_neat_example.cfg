# Test configuration for examples in the early alpha stage of development of the Tensorflow-Neuroevolution framework

[DIRECT_ENCODING]
initializer_kernel      = glorot_normal
initializer_bias        = zeros


[DIRECT_ENCODING_EVOLVABLE]
activation_default      = tanh
activation_out          = sigmoid


[NEAT]
genome_elitism          = 2
reproduction_cutoff     = 0.5
recombine_prob          = 0.2
mutate_weights_prob     = 0.5
add_conn_prob           = 0.15
add_node_prob           = 0.15
initial_connection      = full

species_elitism         = 2
species_max_size        = 20
species_max_stagnation  = 10, 0.05
species_clustering      = threshold-fixed, 12.3
# species_clustering      = threshold-fixed, 12.3, 4
species_interbreeding   = False


[POPULATION]
initial_pop_size        = 10
pop_size_fixed          = False
